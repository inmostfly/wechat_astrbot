# 远程服务器

远程服务器会在离开页面后关闭终端，可以使用这样的方法挂起。

**1. 创建与进入**

| 命令 / 快捷键 | 说明与备注 |
| --- | --- |
| `screen -S <名称>` | **新建并命名会话**（推荐），例如 `screen -S mybot`，方便后续识别与重连。 |
| `screen` | 新建一个未命名的会话（系统默认生成随机 ID）。 |

---

**2. 挂起会话（保持后台运行）**

| 命令 / 快捷键 | 说明与备注 |
| --- | --- |
| `Ctrl + A` 然后按 `d` | **从内部安全挂起**：保持程序在后台继续运行，终端显示 `[detached]` 并退回主终端。 |
| `screen -d <名称>` | **从外部强制挂起**：当会话处于 Attached 状态但无法在内部操作时，在普通终端执行此命令将其切入后台。 |

---

**3. 查看与恢复会话**

| 命令 / 快捷键 | 说明与备注 |
| --- | --- |
| `screen -ls` | **查看所有会话列表**：列出当前运行的所有会话（包含会话 ID、名称及状态 `Attached` 或 `Detached`）。 |
| `screen -r <名称或ID>` | **重新连回会话**：恢复挂起的后台任务（仅适用于 `Detached` 状态的会话）。 |
| `screen -d -r <名称>` | **强制抢占并恢复**：若异常断网导致会话卡在 `Attached` 状态而无法重连时，先踢出原连接再恢复。 |
| `screen -x <名称>` | **多端共享会话**：允许多个终端同时连入同一个 screen 会话，画面实时同步。 |

---

**4. 终止与彻底销毁**

| 命令 / 快捷键 | 说明与备注 |
| --- | --- |
| `exit` / `Ctrl + D` | **在会话内部直接销毁**：在 screen 内停止程序后输入，提示 `[screen is terminating]`，彻底删除该会话。 |
| `screen -X -S <名称> quit` | **在外部直接杀死会话**：无需进入会话，直接在主终端将其强制关闭并销毁。 |
| `screen -wipe` | **清理失效会话**：自动清除进程已被 kill 但仍残留在列表里的 `Dead` 状态会话。 |

---

**标准工作流示例**

```bash
# 1. 新建并进入名为 bot 的会话
screen -S bot

# 2. 在内部运行你的程序
python main.py

# 3. 按 Ctrl+A 再按 d 挂起到后台（此时可放心关闭网页/终端）

# 4. 下次登录后查看会话
screen -ls

# 5. 重新连回查看日志
screen -r bot

# 6. 不需要时在内部输入 exit 彻底销毁
exit

```